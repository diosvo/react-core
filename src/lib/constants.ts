export const HOOKS = {
  useCallback: {
    link: 'https://react.dev/reference/react/useCallback',
    note: 'Cache a function definition between re-renders.',
  },
  useRef: {
    link: 'https://react.dev/reference/react/useRef',
    note: 'Does not trigger a re-render when its value changes, and is stable across renders.',
  },
};
